services:
  - type: web
    name: kap-tarayici
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: kap-data
      mountPath: /data
      sizeGB: 1
